<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name="com.example.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.activities.SearchActivity"
            android:exported="false"
            android:label="@string/search_hint"
            android:theme="@style/Theme.MyApplication"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name="com.example.activities.TranslationActivity"
            android:exported="false"
            android:label="بيئة الترجمة الآلية"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name="com.example.activities.TranslationEditorActivity"
            android:exported="false"
            android:label="محرر الترجمة الاحترافي"
            android:theme="@style/Theme.MyApplication"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
